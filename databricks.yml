bundle:
  name: procaps-framework-ai

sync:
  include:
    - databricks.yml
    - resources/**
    - config/**
    - src/**
    - requirements.txt

variables:
  existing_cluster_id:
    description: ""
    default: "0330-135329-abkt38v6"

targets:
  dev:
    mode: development
    default: true
    workspace:
      host: "https://adb-7020982738702124.4.azuredatabricks.net"
      root_path: /Users/${workspace.current_user.userName}/bundles/${bundle.name}
    run_as:
      user_name: "marlon.marin@dataknow.co"
    variables:
      existing_cluster_id: "0330-135329-abkt38v6"

resources:
  jobs:
    index_workflow_job:
      name: "Procaps automatic indexing workflow [${bundle.target}]"

      tasks:
        - task_key: s00_load_files
          existing_cluster_id: ${var.existing_cluster_id}
          libraries: &common_libraries
            - requirements: "requirements.txt"
          notebook_task:
            notebook_path: "./src/steps/s00_load_files.py"

        - task_key: s01_extract_ocr
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s00_load_files
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s01_extract_ocr.py"

        - task_key: s02_structured_data_processing
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s00_load_files
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s02_structured_data_processing.py"

        - task_key: s03_tabular_processing
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s00_load_files
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s03_tabular_processing.py"

        - task_key: s04_read_images
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s00_load_files
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s04_read_images.py"

        - task_key: s05_process_files
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s01_extract_ocr
            - task_key: s02_structured_data_processing
            - task_key: s03_tabular_processing
            - task_key: s04_read_images
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s05_process_files.py"

        - task_key: s06_indexing
          existing_cluster_id: ${var.existing_cluster_id}
          depends_on:
            - task_key: s05_process_files
          libraries: *common_libraries
          notebook_task:
            notebook_path: "./src/steps/s06_indexing.py"
